{
  "name": "exhaustive-pkg",
  "private": true,
  "version": "0.0.1",
  "main": "lib/exhaustive.js",
  "scripts": {
    "build": "tspc -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "ts-patch": "^3.1.2",
    "typescript": "^5.4.3",
    "transform": "workspace:*"
  }
}
